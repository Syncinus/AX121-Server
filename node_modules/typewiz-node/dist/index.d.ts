import * as tsnode from 'ts-node';
import * as typewiz from 'typewiz-core';
export interface IOptions {
    typewizConfig?: string;
    applyOnExit?: boolean;
    tsNode?: tsnode.Options;
}
export declare function getCollectedTypes(): [string, number, [string | undefined, [string, number] | undefined][], typewiz.IExtraOptions][];
export declare function register(options?: IOptions): void;
